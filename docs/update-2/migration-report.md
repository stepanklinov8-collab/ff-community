# Пробный перенос и перерасчёт

Проверка завершена 24.09.2026. Источник — GET-only снимок от 23.09.2026 10:21 UTC. Все изменения выполнялись только в локальном PGlite.

## Объём

221 профиль, 36 организаций, 150 членств, 5 мероприятий, 13 сессий, 36 игр, 151 заявка, 1 старый результат игры, 14 агрегированных результатов мероприятий, 4 записи личной статистики, 1 запись истории организации, 1 отдельное КВ, 3 предупреждения, 6 рынков, 7 ставок, 256 кошельков.

Применены миграции `202609220001`–`202609220026` и файл полного перерасчёта `supabase/update-2/recalculate.sql`.

Все исходные строки сохранены. Балансы/ставки и исторические результаты не изменены переносом. Не созданы вымышленные личные игровые факты или автоматические публикации. У всех 151 заявок сформирован снимок известных данных состава.

## Распределение основного рейтинга

| Сущности | Количество | Изменились | Минимум до → после | Медиана до → после | Максимум до → после |
|---|---:|---:|---:|---:|---:|
| Игроки | 221 | 0 | 1 → 1 | 1 → 1 | 4 → 4 |
| Организации | 36 | 36 | 1 → 2 | 1 → 2 | 2,19 → 3,1 |

Все значения остаются в диапазоне 1–20. У игроков недостаточно подтверждённой исторической детализации для подстановки в новую формулу: сохранён известный старый игровой рейтинг. У организаций применяется согласованная формула, в том числе компонент собственной репутации. Веса не подгонялись по этому распределению.

## Неполная история

- 14 агрегированных командных результатов без разбивки по играм — сохранены как прежняя история, не превращены в фиктивные игры.
- 1 подтверждённая личная сводка без сессии — сохранена; при появлении детальной публикации неоднозначная сводка не добавляется к ней повторно.
- 3 старых предупреждения без явного размера штрафа — видимы в истории с неизвестным штрафом; новый размер не придуман.
- В 151 заявке прежние имена не были сохранены. Состав восстановлен по зарегистрированным UUID; текущие имена зафиксированы с отметкой времени миграции, а не выданы за достоверные прежние имена.

Эти записи доступны для дальнейшего разбора через приватную `competition_migration_review`.

## Границы проверки

Использованы локальная базовая схема, метаданные колонок REST и выбранные исходные триггеры репозитория. Это не полный SQL dump Supabase. Пользователи Auth представлены локальными идентификаторами. RLS/Storage и ограничения, отсутствующие в REST-метаданных, нужно проверить на staging до выпуска.

Снимок и машинный отчёт лежат только в игнорируемом `tools/database-tests/artifacts/live-copy/`; не включать их в Git, публичные артефакты или веб-каталог.
